// config.js
// -----------------------------------------------------------------------
// URL du backend (API). En local, le backend tourne sur localhost:3000.
// Une fois déployé sur Railway, remplace cette valeur par l'URL publique
// générée (ex. https://tradehubbackend-production.up.railway.app).
// -----------------------------------------------------------------------

const BACKEND_URL = 'http://localhost:3000';
